name: CI

on:
  push:
    branches: [main]
  pull_request:
  workflow_dispatch:

permissions:
  contents: read
  packages: write

jobs:
  ci:
    name: Build & Test
    runs-on: ubuntu-24.04-arm
    steps:
      - name: Checkout
        uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@4d04d5d9486b7bd6fa91e7baf45bbb4f8b9deedd # v4

      - name: Login to GitHub Container Registry
        if: github.event_name == 'push'
        uses: docker/login-action@4907a6ddec9925e35a0a9e82d7399ccc52663121 # v4
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Prepare secrets
        run: |
          echo "${{ secrets.CONFIDENCE_CLIENT_SECRET }}" > /tmp/confidence_client_secret.txt
          echo "${{ secrets.CONFIDENCE_CLIENT_ENCRYPTION_KEY }}" > /tmp/confidence_client_encryption_key.txt

      # Temporary: export Docker-built WASM so it can be committed when local Docker is unavailable.
      - name: Build WASM artifact (PR)
        if: github.event_name != 'push'
        uses: docker/build-push-action@bcafcacb16a39f128d818304e6c9c0c18556b85f # v7
        with:
          context: .
          platforms: linux/arm64
          target: wasm-rust-guest.artifact
          push: false
          outputs: type=local,dest=./wasm-out
          cache-from: type=registry,ref=ghcr.io/${{ github.repository }}/cache:main

      - name: Upload WASM artifact (PR)
        if: github.event_name != 'push'
        uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02 # v4
        with:
          name: confidence_resolver.wasm
          path: ./wasm-out/confidence_resolver.wasm
          if-no-files-found: error

      - name: Build and test everything (PR)
        if: github.event_name != 'push'
        uses: docker/build-push-action@bcafcacb16a39f128d818304e6c9c0c18556b85f # v7
        with:
          context: .
          platforms: linux/arm64
          push: false
          cache-from: type=registry,ref=ghcr.io/${{ github.repository }}/cache:main
          secret-files: |
            confidence_client_secret=/tmp/confidence_client_secret.txt
            confidence_client_encryption_key=/tmp/confidence_client_encryption_key.txt

      - name: Build and test everything (Push - updates cache)
        if: github.event_name == 'push'
        uses: docker/build-push-action@bcafcacb16a39f128d818304e6c9c0c18556b85f # v7
        with:
          context: .
          platforms: linux/arm64
          push: false
          cache-from: type=registry,ref=ghcr.io/${{ github.repository }}/cache:main
          cache-to: type=registry,ref=ghcr.io/${{ github.repository }}/cache:main,mode=max
          secret-files: |
            confidence_client_secret=/tmp/confidence_client_secret.txt
            confidence_client_encryption_key=/tmp/confidence_client_encryption_key.txt
